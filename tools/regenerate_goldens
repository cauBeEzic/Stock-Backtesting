#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
BUILD_DIR="${ROOT_DIR}/build"

cmake -S "${ROOT_DIR}" -B "${BUILD_DIR}" -DBUILD_APP=OFF
cmake --build "${BUILD_DIR}" --target regenerate_goldens
"${BUILD_DIR}/tools/regenerate_goldens" "${ROOT_DIR}"
